// Shared between the (server-only) lib/bookmarks.ts and client-side bookmark UI, so this file
// carries no "server-only" import of its own.
export const MAX_BOOKMARK_NAME_LENGTH = 100;
export const MAX_BOOKMARK_TEXT_LENGTH = 500;
